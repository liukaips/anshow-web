export function RoleMatrix({ roles }: { roles: readonly { id: string; name: string; permissions: readonly string[] }[] }) { return <section className="border border-neutral-200 bg-white" aria-labelledby="role-matrix"><div className="border-b border-neutral-200 px-4 py-4"><h2 id="role-matrix" className="text-lg font-semibold">Roles & permissions</h2></div><div className="grid gap-px bg-neutral-200 sm:grid-cols-2">{roles.map((role) => <article className="bg-white p-4" key={role.id}><h3 className="font-medium">{role.name}</h3><p className="mt-2 text-sm leading-6 text-neutral-600">{role.permissions.join(" · ")}</p></article>)}</div></section>; }
